diff --git a/src/app/globals.css b/src/app/globals.css
index ab38d4e..3c57d40 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -121,4 +121,33 @@ button {
   .section-title {
     font-size: 2rem;
   }
+
+  .container {
+    padding: 0 1.2rem;
+  }
+
+  .btn {
+    padding: 0.75rem 1rem;
+    font-size: 0.9rem;
+  }
+
+  .programsFilterGrid {
+    display: grid;
+    grid-template-columns: 1fr;
+    gap: 1rem;
+    margin-bottom: 1.5rem;
+  }
+
+  .programsSearchField,
+  .programsFilterField {
+    width: 100%;
+  }
+
+  .programsSelect {
+    width: 100%;
+  }
+
+  .programsGridWrapper {
+    grid-template-columns: 1fr;
+  }
 }
diff --git a/src/app/layout.tsx b/src/app/layout.tsx
index be080f4..f783acc 100644
--- a/src/app/layout.tsx
+++ b/src/app/layout.tsx
@@ -4,6 +4,11 @@ import "./globals.css";
 export const metadata: Metadata = {
   title: "Moon TV - Stories that unite a nation",
   description: "Moon TV is a national cultural broadcaster showcasing storytelling and the positive representation of Nigeria.",
+  icons: {
+    icon: "/favicon.png",
+    shortcut: "/favicon.png",
+    apple: "/favicon.png",
+  },
 };
 
 export default function RootLayout({
diff --git a/src/app/programs/page.tsx b/src/app/programs/page.tsx
index 1124726..6e7f805 100644
--- a/src/app/programs/page.tsx
+++ b/src/app/programs/page.tsx
@@ -28,40 +28,50 @@ export default function Programs() {
                     <p style={{ color: "var(--text-secondary)", marginBottom: "3rem", maxWidth: "600px", fontSize: "1.1rem" }}>
                         From beloved classics to thrilling contemporary series, explore content that reflects our pride, dignity, and cultural richness.
                     </p>
-                    <div style={{ display: "flex", gap: "1rem", marginBottom: "1rem", flexWrap: "wrap", alignItems: "center" }}>
-                        <input
-                            type="text"
-                            placeholder="Search programmes..."
-                            value={searchTerm}
-                            onChange={(e) => setSearchTerm(e.target.value)}
-                            style={{
-                                flex: "1",
-                                minWidth: "240px",
-                                padding: "0.85rem 1rem",
-                                borderRadius: "8px",
-                                border: "1px solid var(--border-color)",
-                                backgroundColor: "var(--bg-secondary)",
-                                color: "white"
-                            }}
-                        />
-                        <span style={{ color: "var(--text-secondary)", fontSize: "0.95rem", whiteSpace: "nowrap" }}>
-                            Showing {filteredProgrammes.length} of {programmes.length} programmes
-                        </span>
+                    <div className="programsFilterGrid">
+                        <div className="programsSearchField">
+                            <label htmlFor="searchProgrammes" style={{ color: "var(--text-secondary)", fontSize: "0.95rem", fontWeight: 600 }}>Search programmes</label>
+                            <input
+                                id="searchProgrammes"
+                                type="text"
+                                placeholder="Search programmes..."
+                                value={searchTerm}
+                                onChange={(e) => setSearchTerm(e.target.value)}
+                                style={{
+                                    width: "100%",
+                                    padding: "1rem 1.1rem",
+                                    borderRadius: "16px",
+                                    border: "1px solid rgba(255,255,255,0.12)",
+                                    backgroundColor: "rgba(255,255,255,0.04)",
+                                    color: "white",
+                                    minWidth: 0,
+                                    outline: "none"
+                                }}
+                            />
+                        </div>
+                        <div className="programsFilterField">
+                            <label htmlFor="categoryFilter" style={{ color: "var(--text-secondary)", fontSize: "0.95rem", fontWeight: 600 }}>Category</label>
+                            <div style={{ position: "relative" }}>
+                                <select
+                                    id="categoryFilter"
+                                    value={activeCategory}
+                                    onChange={(e) => setActiveCategory(e.target.value)}
+                                    className="programsSelect"
+                                >
+                                    {programmeCategories.map((cat) => (
+                                        <option key={cat} value={cat}>{cat}</option>
+                                    ))}
+                                </select>
+                                <div className="programsSelectArrow">Γû╝</div>
+                            </div>
+                        </div>
                     </div>
-                    <div style={{ display: "flex", gap: "0.75rem", marginBottom: "3rem", flexWrap: "wrap" }}>
-                        {programmeCategories.map((cat) => (
-                            <button
-                                key={cat}
-                                onClick={() => setActiveCategory(cat)}
-                                className={activeCategory === cat ? "btn btn-green" : "btn btn-outline-gold"}
-                                style={{ padding: "0.5rem 1rem", fontSize: "0.8rem" }}
-                            >
-                                {cat}
-                            </button>
-                        ))}
-                    </div>
-                    <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(320px, 1fr))", gap: "2rem" }}>
-                        {programmes.map((prog) => (
+                    <div className="programsGridWrapper">
+                        {filteredProgrammes.length === 0 ? (
+                            <div style={{ gridColumn: "1 / -1", textAlign: "center", color: "var(--text-secondary)", padding: "3rem 1rem", borderRadius: "16px", backgroundColor: "var(--bg-secondary)", border: "1px solid var(--border-color)" }}>
+                                No programmes match your search or category yet.
+                            </div>
+                        ) : filteredProgrammes.map((prog) => (
                             <a href={`/program/${prog.slug}`} key={prog.id} style={{ display: "block" }}>
                                 <div style={{ backgroundColor: "var(--bg-secondary)", borderRadius: "8px", overflow: "hidden", border: "1px solid var(--border-color)", transition: "border-color 0.3s", height: "100%", display: "flex", flexDirection: "column" }}>
                                     <div style={{ width: "100%", height: "200px", position: "relative", backgroundColor: "#0b1a0e" }}>
diff --git a/src/components/Header.tsx b/src/components/Header.tsx
index f81d28d..4fafc98 100644
--- a/src/components/Header.tsx
+++ b/src/components/Header.tsx
@@ -6,6 +6,7 @@ import Image from 'next/image';
 
 export default function Header({ isInner = false }: { isInner?: boolean }) {
     const [user, setUser] = useState<{ username: string } | null>(null);
+    const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
 
     useEffect(() => {
         const stored = localStorage.getItem('moon_user');
@@ -21,10 +22,14 @@ export default function Header({ isInner = false }: { isInner?: boolean }) {
     const handleLogout = () => {
         localStorage.removeItem('moon_user');
         setUser(null);
+        setMobileMenuOpen(false);
         alert('You have logged out.');
         window.location.reload();
     };
 
+    const toggleMobileMenu = () => setMobileMenuOpen((prev) => !prev);
+    const closeMobileMenu = () => setMobileMenuOpen(false);
+
     return (
         <header className="header" style={isInner ? { position: 'relative', backgroundColor: 'var(--bg-primary)' } : {}}>
             <div className="container headerInner">
@@ -32,6 +37,14 @@ export default function Header({ isInner = false }: { isInner?: boolean }) {
                     <Image src="/logo.png" alt="Moon TV Logo" width={40} height={40} className="logoImage" />
                     <span>MOON TV</span>
                 </Link>
+                <button
+                    className="mobileNavToggle"
+                    onClick={toggleMobileMenu}
+                    aria-label={mobileMenuOpen ? 'Close navigation menu' : 'Open navigation menu'}
+                    type="button"
+                >
+                    {mobileMenuOpen ? 'Γ£ò' : 'Γÿ░'}
+                </button>
                 <nav className="nav">
                     <Link href="/programs" className="navLink">Programmes</Link>
                     <Link href="/epg" className="navLink">Schedule</Link>
@@ -57,6 +70,20 @@ export default function Header({ isInner = false }: { isInner?: boolean }) {
                     <button className="btn btn-green">Watch Live</button>
                 </div>
             </div>
+            <div className={`mobileMenu ${mobileMenuOpen ? 'open' : ''}`}>
+                <div className="mobileMenuContent">
+                    <Link href="/programs" className="mobileMenuLink" onClick={closeMobileMenu}>Programmes</Link>
+                    <Link href="/epg" className="mobileMenuLink" onClick={closeMobileMenu}>Schedule</Link>
+                    <Link href="/about" className="mobileMenuLink" onClick={closeMobileMenu}>About</Link>
+                    <Link href="/advertise" className="mobileMenuLink" onClick={closeMobileMenu}>Advertise</Link>
+                    <Link href="/contact" className="mobileMenuLink" onClick={closeMobileMenu}>Contact</Link>
+                    {user ? (
+                        <button className="mobileMenuAction" type="button" onClick={handleLogout}>Log Out</button>
+                    ) : (
+                        <Link href="/login" className="mobileMenuAction" onClick={closeMobileMenu}>Sign In</Link>
+                    )}
+                </div>
+            </div>
         </header>
     );
 }
